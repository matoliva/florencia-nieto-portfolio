---
import { Image } from 'astro:assets';

interface Props {
  img: string;
  width: number;
  height: number;
  href: string;
  linkText: string;
}

const { img, width, height, href, linkText } = Astro.props;
---

<div class='card'>
  <a class='card__img' {href}>
    <Image src={img} alt='Project' {width} {height} />
  </a>
  <div class='card__link'>
    <h2><a {href}>{linkText}</a></h2>
  </div>
</div>
<style>
  .card {
    position: relative;
  }

  .card:hover {
    cursor: pointer;
  }

  .card__img img {
    object-fit: cover;
    filter: grayscale(0); /* No grayscale by default */
    transition: filter 0.7s ease; /* Smooth transition */
  }

  .card:hover .card__img img {
    filter: grayscale(100%);
    cursor: pointer;
  }

  .card__link {
    position: absolute;
    bottom: 0;
    width: 100%;
    padding: 1rem;
    background-color: rgba(0, 0, 0, 0.5); /* Black background with 50% opacity */
  }

  .card__link h2 {
    font-size: 2rem;
    font-weight: 400;
  }

  .card__link a {
    text-decoration: none;
    color: var(--bg-color);
  }

  .card__link a:hover {
    text-decoration-line: underline;
  }

  @media (max-width: 768px) {
    .card__link {
      padding: 0.5rem;
    }
    .card__link h2 {
      font-size: 1.5rem;
    }
  }
</style>
